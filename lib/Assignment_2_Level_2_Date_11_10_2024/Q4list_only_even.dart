

// write a dart code that takes this list and makes a new list that has only the odd elements for this list in it.

// Start
import 'dart:io';
void main(){
  List<int> listOne = List.generate(25,(index) => index * 2);
  print(listOne);
}