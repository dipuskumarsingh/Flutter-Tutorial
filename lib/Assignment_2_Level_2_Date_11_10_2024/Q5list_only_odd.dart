

// Write a Dart code that takes this list and makes a new list that has only the odd elements of this list in it.

//Start
void main(){
  List<int> listOne = List.generate(25, (index) => index + (3-1));
    print(listOne);
}
// Question not done